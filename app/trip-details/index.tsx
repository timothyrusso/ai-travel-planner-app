import { Text, View } from 'react-native';

export const index = () => {
  // Retrieve trip details:
  // const { trip } = useLocalSearchParams()
  // JSON.parse(trip)

  return (
    <View>
      <Text>index</Text>
    </View>
  );
};
