import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  // The mask owns the rounded shape on Android, so the blur itself just fills it.
  androidBlur: {
    flex: 1,
    width: '100%',
  },
});
